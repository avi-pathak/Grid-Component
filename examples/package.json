{
  "name": "apgrid-demos",
  "version": "0.0.0",
  "private": true,
  "description": "Demo app that consumes @avi-pathak/apgrid as a real npm package, installed from a local `npm pack` tarball (see `npm run pack:examples` at the repo root) rather than the registry — so it always reflects the current source, not the last published version.",
  "scripts": {
    "dev": "webpack serve --config webpack.config.js",
    "build": "webpack --config webpack.config.js --mode production",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@avi-pathak/apgrid": "file:./apgrid-local.tgz"
  },
  "devDependencies": {
    "css-loader": "^6.10.0",
    "html-webpack-plugin": "^5.6.0",
    "sass": "^1.101.0",
    "sass-loader": "^16.0.8",
    "style-loader": "^3.3.4",
    "ts-loader": "^9.5.1",
    "typescript": "^5.4.5",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^5.0.4"
  }
}
